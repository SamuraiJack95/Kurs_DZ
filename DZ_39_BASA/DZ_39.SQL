--1.
--SELECT KL.NAME_KL, KL.CITY_KL, KL.OBL_KL, PR.NAME_PR, PR.CITY_PR
--FROM KLIENT KL INNER JOIN Prodaves PR ON (KL.ID_PR = PR.ID_PR)  

--2.
--SELECT KL.NAME_KL, Z.SUMMA, IIF (Z.SUMMA > (SELECT AVG(SUMMA) FROM ZAKAZ), "FEW", "LOW") AS AVGG
--FROM ZAKAZ Z INNER JOIN KLIENT KL ON (Z.ID_KL = KL.ID_KL)

--3.
--SELECT *
--FROM ZAKAZ Z FULL OUTER JOIN PRODAVES PR ON (Z.ID_PR = PR.ID_PR) FULL OUTER JOIN KLIENT KL ON (PR.ID_PR = KL.ID_PR)

--4.
--SELECT DISCOUN, NAME_PR, NAME_KL
--FROM PRODAVES PR OUTER LEFT JOIN KLIENT KL ON (PR.ID_PR = KL.ID_PR)

--5.
--SELECT CITY_KL
--FROM KLIENT
--
--UNION
--
--SELECT CITY_PR
--FROM PRODAVES

--6.
--SELECT *
--FROM KLIENT KL LEFT OUTER JOIN ZAKAZ Z --ON (KL.ID_KL = Z.ID_KL)
--WHERE DATA = '1996-10-04 00:00:00'

--7.
--SELECT ID_PR, NAME_PR
--FROM PRODAVES PR INNER JOIN KLIENT K ON (PR.ID_PR = K.ID_PR) INNER JOIN ZAKAZ Z ON (Z.ID_KL = K.ID_KL)
--WHERE K.CITY_KL NOT IN (SELECT ID_KL FROM ZAKAZ)

--8.
--SELECT  NAME_KL
--FROM KLIENT
--WHERE CITY_KL = "London"
--
--UNION
--
--SELECT NAME_PR
--FROM PRODAVES
--WHERE CITY_PR = "London"
